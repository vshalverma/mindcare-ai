# mindcare-ai dependencies
# Generated to match the working venv on the dev box (GTX 1650, CUDA 12.x).
# Versions are pinned to a known-working set; the major-version floors are
# what actually matter.

# --- Data pipeline ---
datasets>=2.14
pandas>=2.0
numpy>=1.24
scikit-learn>=1.3
pyarrow>=12.0
pyyaml>=6.0

# --- Training & inference (torch CUDA build is installed via the venv,
#     but we keep torch in the file so a fresh venv can be recreated). ---
torch>=2.1
transformers>=4.40

# --- UI ---
streamlit>=1.32

# --- Misc ---
safetensors>=0.4        # used by the classifier loader

# --- Tests (dev only; the app doesn't import pytest at runtime) ---
pytest>=8.0
